#!/bin/bash

echo $#

if (( $# != 1 )) ; then
    echo "USAGE: headnode.bash <crun_type>"
    exit 1
fi

CRUNTYPE=$1

./headnode.bash --no-cleanup -C $CRUNTYPE -p ~/chris/lib/py -r ~/chris/src/cruntesting -s computenode.py -c 20 -m 10 -t ~/scratch ls /tmp 2>/dev/null

